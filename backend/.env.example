# Database
# Neon connection string — use the pooled connection string from your Neon dashboard
DATABASE_URL=""

# Auth
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=""

# Server
PORT=8000
NODE_ENV="development"